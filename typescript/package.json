{
	"name": "stricttest",
	"version": "0.1.1",
	"type": "module",
	"description": "An always-on test-isolation floor for Node suites: throwaway HOME and XDG directories, an isolated git config and identity, transport lockdown, credential stripping, and sandbox enforcement",
	"engines": {
		"node": ">=22"
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/smm-h/stricttest.git",
		"directory": "typescript"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"types": "./dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"test": "npm run build && tsc -p tsconfig.test.json && node --test \"dist-test/tests/**/*.test.js\"",
		"prepack": "npm run build"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"typescript": "^7.0.2"
	},
	"keywords": [
		"testing",
		"isolation",
		"sandbox",
		"security",
		"node-test",
		"rlsbl"
	]
}
